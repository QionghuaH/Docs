1.选堆快希不稳,选堆归基不变    不稳是指不稳定，不变是（运行时间不发生变化，与初始状态无关）
2.记住两个公式：队列中，队列满的条件是：（rear+1）%QueueSize=front；
                       队列长度公式是：（rear-front+QueueSize）%QueueSize。
3.一个非空广义表的表尾()  子表
4.线性表有两种存储结构：
  1.顺序存储结构---顺序表。顺序表以数组形式出现，可以取任意下标访问，所以是一种随机存取的存储结构。
  2.链式存储结构---链表。链表以链表的形式出现，必须从头开始访问，所以是一种顺序存取的存储结构。
